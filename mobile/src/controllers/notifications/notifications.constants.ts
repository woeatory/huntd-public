import { PushNotificationPermissions } from 'react-native-push-notification';

export const PUSH_NOTIFICATIONS_TOKEN = 'PUSH_NOTIFICATIONS_TOKEN';

export const PUSH_NOTIFICATIONS_PERMISSIONS: PushNotificationPermissions = {
  alert: true,
  badge: true,
  sound: true,
};
